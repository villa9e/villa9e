'use client';
// BankBottomNav is disabled per spec — the global teepee handles all navigation.
// The component is kept so imports compile, but renders nothing.
export function BankBottomNav({ active }: { active: string }) {
  return null;
}
